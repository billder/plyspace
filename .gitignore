# Dependencies
node_modules/

# Environment variables (contains Stripe keys, admin password)
.env

# Database (contains order/product data — back up separately)
zines.db

# Uploaded zine cover images (store in cloud or back up separately)
public/uploads/*
!public/uploads/.gitkeep

# Mac junk
.DS_Store

# Logs
*.log
npm-debug.log*
